import React from 'react'
import './css/player.css'

const Player = props => (
              <div className='playerLayout'>
                  {props.children}
              </div>
        )
export default Player
